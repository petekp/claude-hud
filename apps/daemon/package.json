{
  "name": "@claude-hud/daemon",
  "version": "0.1.0",
  "description": "HUD daemon for Claude Code state tracking and relay",
  "type": "module",
  "main": "dist/cli/index.js",
  "bin": {
    "hud-claude": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/cli/index.js",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
